// importing the react and allows us to took this react application to index.html 
import React from "react";
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')) // render the application by saying document.getElementById that's going to be root
